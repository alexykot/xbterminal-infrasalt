sentry:
  key: 'key'
  crt: 'crt'
  
##main config
  config:
    DATABASES:
      default:
        ENGINE: 'sentry.db.postgres'
        NAME: 'sentrydb'
        USER: 'sentry_usr'
        PASSWORD: 'zX'
        HOST: '127.0.0.1'
        PORT: 5432

#new style config
    yml:
      mail.backend: dummy
      redis.clusters:
        default:
          hosts:
            0:
              host: 127.0.0.1
              port: 6379
              db: 4
